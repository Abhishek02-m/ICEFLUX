export default function Settings() {
  return (
    <section className="page active" id="page-settings">
      <div className="page-head"><div><h1>Settings</h1><p>Account and workspace preferences</p></div></div>
      <div className="empty-state">
        <h2>Settings isn't built yet</h2>
        <p>Account, notification, and unit preferences will live here. You're signed in as a demo user, so nothing here would be saved yet.</p>
      </div>
    </section>
  );
}
